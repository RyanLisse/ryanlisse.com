Start the ryanlisse.com development server with Turbopack.

```bash
npm run dev
```

The dev server starts at http://localhost:3000.

Turbopack is enabled (Next.js 16). Pages hot-reload on save.

After starting, confirm the server is up and report the local URL. If port 3000 is in use, note the actual port Next.js picks.
